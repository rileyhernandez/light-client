[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --chip RP2040"
[build]
target = "thumbv6m-none-eabi"

[env]
DEFMT_LOG = "info"
# WIFI_SSID = "*borat voice* my wifi"
# WIFI_PASSWORD = "2034517528"
WIFI_SSID = "Riley"
WIFI_PASSWORD = "hernandez"